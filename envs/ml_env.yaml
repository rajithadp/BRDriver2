name: cancer_driver_ml
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas
  - pyyaml
  - scikit-learn
  - joblib
  - xgboost
  - imbalanced-learn
  - tabulate
  - matplotlib
  - pip
  # For external feature retrieval (if needed later)
  # - pyensembl